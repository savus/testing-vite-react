import { Modal } from "./Modal";
import { Navbar } from "./Navbar";

export const Header = () => {
  return (
    <header className="main-header">
      <Navbar>something</Navbar>
      <Modal></Modal>
    </header>
  );
};
